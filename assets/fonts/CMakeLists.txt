set( LATIN_RANGE "20,7F,A1,180" )
set( FONT_FILE "${CMAKE_CURRENT_SOURCE_DIR}/DejaVuSans-Bold.ttf" )

cook_font( SRC ${FONT_FILE} TARGET dejavu SIZE 24 RANGES ${LATIN_RANGE} )
cook_font( SRC ${FONT_FILE} TARGET dejavu SIZE 36 RANGES ${LATIN_RANGE} )
cook_font( SRC ${FONT_FILE} TARGET dejavu SIZE 64 RANGES ${LATIN_RANGE} )
