set( LATIN "20,7F,A1,180" )
set( FONT_LATIN "${CMAKE_CURRENT_SOURCE_DIR}/DejaVuSans-Bold.ttf" )
cook_font( SRC ${FONT_LATIN} NAME small TARGET small_latin SIZE 24 RANGES ${LATIN} )
cook_font( SRC ${FONT_LATIN} NAME medium TARGET medium_latin SIZE 36 RANGES ${LATIN} )
cook_font( SRC ${FONT_LATIN} NAME large TARGET large_latin SIZE 64 RANGES ${LATIN} )

set( LATIN_EX "1E00,1F00" )
set( FONT_LATIN_EX "${CMAKE_CURRENT_SOURCE_DIR}/DejaVuSans-Bold.ttf" )
cook_font( SRC ${FONT_LATIN_EX} NAME small TARGET small_latin_ex SIZE 24 RANGES ${LATIN_EX} )
cook_font( SRC ${FONT_LATIN_EX} NAME medium TARGET medium_latin_ex SIZE 36 RANGES ${LATIN_EX} )
cook_font( SRC ${FONT_LATIN_EX} NAME large TARGET large_latin_ex SIZE 64 RANGES ${LATIN_EX} )

set( CYRILIC "400,500" )
set( FONT_CYRILIC "${CMAKE_CURRENT_SOURCE_DIR}/DejaVuSans-Bold.ttf" )
cook_font( SRC ${FONT_CYRILIC} NAME small TARGET small_cyrilic SIZE 24 RANGES ${CYRILIC} )
cook_font( SRC ${FONT_CYRILIC} NAME medium TARGET medium_cyrilic SIZE 36 RANGES ${CYRILIC} )
cook_font( SRC ${FONT_CYRILIC} NAME large TARGET large_cyrilic SIZE 64 RANGES ${CYRILIC} )

# set( THAI "E01,E3B,E3F,E5C" )
# set( FONT_THAI "${CMAKE_CURRENT_SOURCE_DIR}/NotoSansThai-Bold.ttf" )
# cook_font( SRC ${FONT_THAI} NAME small TARGET small_thai SIZE 24 RANGES ${THAI} )
# cook_font( SRC ${FONT_THAI} NAME medium TARGET medium_thai SIZE 36 RANGES ${THAI} )
# cook_font( SRC ${FONT_THAI} NAME large TARGET large_thai SIZE 64 RANGES ${THAI} )

# set( KANA "3041,3097,3099,3100" )
# set( FONT_JP "${CMAKE_CURRENT_SOURCE_DIR}/NotoSansJP-Bold.ttf" )
# cook_font( SRC ${FONT_JP} NAME small TARGET small_jp SIZE 24 RANGES ${KANA} )
# cook_font( SRC ${FONT_JP} NAME medium TARGET medium_jp SIZE 36 RANGES ${KANA} )
# cook_font( SRC ${FONT_JP} NAME large TARGET large_jp SIZE 64 RANGES ${KANA} )

# set( JAVA "A980,A9CE,A9CF,A9DA,A9DE,A9E0" )
# set( FONT_JAVA "${CMAKE_CURRENT_SOURCE_DIR}/NotoSansJavanese-Bold.ttf" )
# cook_font( SRC ${FONT_JAVA} NAME small TARGET small_jv SIZE 24 RANGES ${JAVA} )
# cook_font( SRC ${FONT_JAVA} NAME medium TARGET medium_jv SIZE 36 RANGES ${JAVA} )
# cook_font( SRC ${FONT_JAVA} NAME large TARGET large_jv SIZE 64 RANGES ${JAVA} )
